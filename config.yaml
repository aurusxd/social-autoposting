telegram:
  channels:
    - id: "-1003995989220"
      name: "Основной канал"

whatsapp:
  # Groups and channels are read from Whapi at runtime
  enabled: true

instagram:
  enabled: true

tiktok:
  enabled: true
